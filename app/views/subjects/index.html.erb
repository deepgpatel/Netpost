<h1>Subjects</h1>

#  <%= collection_check_boxes(:user, :subject_ids, Subject.all, :id, :title) %>


<% @subjects.each do |subject| %>
<li><%= link_to subject.title, subject_path(subject) %></li>
<% end %>