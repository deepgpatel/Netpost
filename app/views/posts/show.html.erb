<h3><%= @post.title %></h3>
<p><%= @post.content %></p>


<%= form_for @comment, url: post_path(@post), method: "patch" do |f| %>
  Add Comment: <%= f.text_area :content %> 
  <%= f.submit %>
  <% end %>

 <% @post.comments.each do |comment| %>
   <%= comment.content %>
 <% end %>